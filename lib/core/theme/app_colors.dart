import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color.fromRGBO(25, 117, 255, 1);
  static const Color secondary = Color(0xFF03A9F4);

  // Background colors
  static const Color background = Colors.black;
  static const Color surface = Colors.white;
  static const Color white = Colors.white;

  static const Color transparent = Colors.transparent;

  // Text colors
  static const Color textPrimary = Color.fromRGBO(0, 0, 0, 0.85);
  static const Color textSecondary = Color.fromRGBO(0, 0, 0, 0.5);

  // Status colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color.fromRGBO(64, 191, 127, 1);
  static const Color warning = Color.fromRGBO(255, 178, 26, 1);

  // Additional colors
  static const Color divider = Color(0xFFBDBDBD);
  static const Color blue = Color.fromRGBO(74, 144, 226, 1);
  static const Color disabled = Color(0xFF9E9E9E);
  static const Color bgContainer = Color.fromRGBO(251, 251, 251, 1);
  static const Color purple = Color.fromRGBO(128, 83, 255, 1);
  static const Color yellow = Color.fromRGBO(255, 178, 26, 1);
  static const Color border = Color.fromRGBO(228, 231, 236, 1);
  static const Color teal = Color(0xFF009688);
  static const Color indigo = Color(0xFF3F51B5);
  static const Color pink = Color(0xFFE91E63);
  static const Color cyan = Color(0xFF00BCD4);
  static const Color amber = Color(0xFFFFC107);

  // Additional colors for form fields
  static const Color fieldBackground = Color.fromRGBO(245, 245, 245, 1);
  static const Color hintText = Color.fromRGBO(0, 0, 0, 0.4);
  static const Color iconColor = Color.fromRGBO(0, 0, 0, 0.6);
}
